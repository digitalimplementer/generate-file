export enum EOrderStatus {
  AWAITING = 'awaiting',
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
  FAIL = 'fail',
  ERROR = 'error',
}

export enum EOrderMode {
  AUTO = 'auto',
  MANUAL = 'manual',
}
